{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Docker: Attach to Node",
      "port": 9229,
      "address": "localhost",
      // windowsの方は↓のコメントアウトをはずしてください
      "localRoot": "${workspaceFolder}//myapp",
      // macの方は↓のコメントアウトをはずしてください
      // "localRoot": "${workspaceFolder}/myapp",
      "remoteRoot": "/usr/src/app/"
    }
  ]
}
